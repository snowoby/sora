const account = "/account/";
const profile = "/profile/";

export default {
  account,
  login: account + "login",
  register: account + "register",
  refresh: account + "refresh",
  self: account + "",
  profile,
};
